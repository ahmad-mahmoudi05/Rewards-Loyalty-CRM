import { PlaceholderPage } from "@/components/dashboard/placeholder-page";

export default function CampaignsPage() {
  return (
    <PlaceholderPage
      title="Campaigns"
      description="Pick an audience segment, a channel (WhatsApp, SMS, or email), and a message — see the eligible, consented recipient count before you send."
      plannedFor="Planned for Day 4."
    />
  );
}
