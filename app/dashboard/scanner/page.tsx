import { PlaceholderPage } from "@/components/dashboard/placeholder-page";

export default function ScannerPage() {
  return (
    <PlaceholderPage
      title="Scanner"
      description="Staff mode: scan a customer's Wallet QR (or search by phone/name), see their loyalty progress and available rewards, then confirm a transaction or redemption."
      plannedFor="Planned for Day 3."
    />
  );
}
